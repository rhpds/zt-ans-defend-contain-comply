---
- name: Push container image to registry
  hosts: rhel01
  gather_facts: false

  vars:
    container_name: parasol-app
    container_tag: latest
    registry_url: "localhost:5000"
    verify_after_push: true

  tasks:
    - name: Check if registry container is running
      ansible.builtin.command:
        cmd: podman --cgroup-manager=cgroupfs inspect --format '{{ '{{' }}.State.Running{{ '}}' }}' workshop-registry
      register: _registry_running
      changed_when: false
      failed_when: false

    - name: Start local registry container
      ansible.builtin.command:
        cmd: >-
          podman --cgroup-manager=cgroupfs run -d
          --name workshop-registry
          -p 5000:5000
          -v registry-data:/var/lib/registry
          docker.io/library/registry:2
      when: _registry_running.stdout | default('') != 'true'
      register: _registry_container

    - name: Wait for registry to be available
      ansible.builtin.uri:
        url: "http://{{ registry_url }}/v2/"
        status_code: [200, 401]
      retries: 5
      delay: 3
      register: _registry_health
      until: _registry_health is succeeded

    - name: Push image to local registry
      ansible.builtin.command:
        cmd: >-
          podman --cgroup-manager=cgroupfs push --tls-verify=false
          {{ registry_url }}/{{ container_name }}:{{ container_tag }}
      register: _push_result

    - name: Verify image exists in registry
      ansible.builtin.uri:
        url: "http://{{ registry_url }}/v2/{{ container_name }}/tags/list"
        return_content: true
      register: _registry_tags
      when: verify_after_push | bool

    - name: Assert image tag is present in registry
      ansible.builtin.assert:
        that:
          - container_tag in (_registry_tags.json.tags | default([]))
        fail_msg: "Image tag {{ container_tag }} not found in registry after push."
        success_msg: "Image {{ container_name }}:{{ container_tag }} confirmed in registry."
      when: verify_after_push | bool

    - name: Run container to verify it serves content
      ansible.builtin.command:
        cmd: >-
          podman --cgroup-manager=cgroupfs run -d
          --replace --name {{ container_name }}-verify
          -p 8443:8443
          {{ registry_url }}/{{ container_name }}:{{ container_tag }}
      register: _verify_container

    - name: Wait for application container to respond
      ansible.builtin.uri:
        url: "http://localhost:8443/"
        status_code: [200, 403, 500]
      retries: 5
      delay: 3
      register: _app_health
      until: _app_health is succeeded

    - name: Verify application serves expected content
      ansible.builtin.assert:
        that:
          - _app_health.status == 200
          - "'Parasol Finance' in _app_health.content"
        success_msg: "Application container is serving content correctly."
      ignore_errors: true
      register: _content_check

    - name: Warn if container responds but content check failed
      ansible.builtin.debug:
        msg: >-
          Container is running (HTTP {{ _app_health.status }}) but content check did not pass.
          Rebuild the container image to fix httpd configuration.
      when: _content_check is failed

    - name: Remove verification container
      ansible.builtin.command:
        cmd: podman --cgroup-manager=cgroupfs rm -f {{ container_name }}-verify
      changed_when: true

    - name: Report push and verification results
      ansible.builtin.debug:
        msg: |
          Supply chain delivery complete:
            Registry: {{ registry_url }}
            Image: {{ container_name }}:{{ container_tag }}
            Tags in registry: {{ _registry_tags.json.tags | default([]) | join(', ') }}
            Application verified: healthy
