#!/bin/bash
# Module 01 (DEFEND) -- no runtime transition needed.
# All DEFEND resources (Scan System, Contain Service, Harden Posture,
# Vulnerability Containment workflow) are created during initial setup.
echo "Module 01 (DEFEND): pre-configured at deploy time, no transition needed."
